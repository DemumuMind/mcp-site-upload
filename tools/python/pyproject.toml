[project]
name = "mcp-site-automation"
version = "0.1.0"
description = "Python automation tools for mcp-site (reporting and catalog enrichment)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = []
